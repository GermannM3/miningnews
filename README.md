# Telegram Mining News Bot

Телеграм-бот для ручного запуска, собирающий новости о металлургии, ГОКах, стали и декарбонизации с десятков RSS- и HTML-источников. Новости публикуются сначала в канал-предпросмотр для модерации.

## Требования
- Python 3.11+
- Системные библиотеки для Playwright (установятся командой ниже)

## Установка
```bash
cd MiningNewsBot
python3 -m venv .venv        # опционально
source .venv/bin/activate    # если создали venv
pip install -r requirements.txt
python -m playwright install --with-deps
```

## Настройка окружения
Создайте файл `.env` (или задайте переменные в системе):
```env
BOT_TOKEN=8478...                # токен бота из @BotFather
CHANNEL_ID=-1001234567890        # ID основного канала
PREVIEW_CHANNEL_ID=-100987654321 # ID канала-предпросмотра
CHECK_INTERVAL=600               # интервал между циклами (секунды)
MAX_NEWS_PER_SOURCE=5            # максимум новостей с источника
STATIC_PROXY=socks5://127.0.0.1:10808   # опционально, приоритетный прокси
PROXY_SOURCE_URL=https://www.proxy-list.download/api/v1/get?type=https
PLAYWRIGHT_HEADLESS=true
PLAYWRIGHT_TIMEOUT=40000
```

Обязательны первые три переменные. Остальные можно не задавать – используются значения по умолчанию.

## Запуск

**Важно**: На Alt Linux используйте `python3` вместо `python`.

### Самый простой способ (рекомендуется):
```bash
cd /home/germannm/Видео/MiningNewsBot
./start_bot.sh
```

### Остановка бота:
```bash
./stop_bot.sh
```

### Ручной запуск (с виртуальным окружением):
```bash
cd /home/germannm/Видео/MiningNewsBot
source .venv/bin/activate
set -a
source .env
set +a
python3 main.py
```

### Альтернативный запуск (одной командой):
```bash
cd /home/germannm/Видео/MiningNewsBot && source .venv/bin/activate && set -a && source .env && set +a && python3 main.py
```

Бот выполняет один цикл сразу и далее повторяет его каждые `CHECK_INTERVAL` секунд. Остановите `Ctrl+C` (или используйте `./stop_bot.sh`).

## Прокси / VLESS

⚠️ **Важно**: Бесплатные прокси нестабильны и вызывают множество ошибок. Рекомендуется работать либо без прокси, либо использовать платный/собственный прокси.

### Варианты работы:

**1. Без прокси (рекомендуется для начала):**
```bash
# В .env закомментируйте обе строки прокси:
#PROXY_SOURCE_URL=...
#STATIC_PROXY=...
```

**2. С платным HTTP/SOCKS5 прокси:**
```bash
# В .env раскомментируйте и укажите свой прокси:
STATIC_PROXY=socks5://user:pass@proxy.example.com:1080
```

**3. С VLESS (ваш прокси уже настроен):**
```bash
# 1. Установите Xray локально (без root):
./install_xray_local.sh

# 2. Прокси уже активирован в .env:
STATIC_PROXY=socks5://127.0.0.1:10808

# 3. Запустите бота (поднимет Xray автоматически):
./start_bot.sh
```

📖 Подробные инструкции: см. файл `SETUP_VLESS.md`

💡 Совет: используйте `./quick_start.sh` для полного цикла проверки → установки → запуска.

### Источники, требующие прокси/VPN:
Без прокси недоступны следующие источники (заблокированы или требуют VPN):
- Vale, Tata Steel, POSCO, Baowu, HbisCo, MySteel, ArcelorMittal
- Kallanish, BigMint, Metal.com, ЕВРАЗ, Металлоинвест

Остальные 15+ источников работают стабильно без прокси.

## Структура проекта
```
main.py          — основной сценарий
config.py        — чтение переменных окружения
filters.py       — фильтр релевантности новостей
sources.py       — список источников и селекторы
setup_vless.sh   — быстрая настройка локального VLESS-прокси
requirements.txt — зависимости проекта
duplicates.txt   — хранилище хэшей опубликованных ссылок
```

## Полезно знать
- Для Playwright нужен Chromium: `python -m playwright install --with-deps`.
- Логи пишутся в stdout; файл `bot.log` очищается после каждого цикла.
- `duplicates.txt` можно удалить для сброса истории (например, перед запуском на новой площадке).
# miningnews
